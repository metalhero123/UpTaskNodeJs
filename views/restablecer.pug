<!DOCTYPE html>
html(lang="en")
    head
        include partials/_header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Agrega tu e-mail para resteblecer la contraseña
            
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p=error

            form.caja-login(method="POST" action="/restablecer")
                .campo
                    label(form="email") E-mail:
                    input(type="email" name="email" placeholder="Tu email" value= email)
                .campo.enviar
                    input.boton(type="submit" value="Restablecer contraseña")
                .campo.acciones
                    a(href="/iniciar-sesion") Volver al login